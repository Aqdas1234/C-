using System;

namespace std{
    class variavbl{
        static void Main(string[] ags)
        {
            //  c# is typed language means we need specify 
            //the type of a variable at the time of  decleration

            int a ;
            a=5;
            Console.WriteLine(a);
        }
    }
}